<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings - CINE-BOOK</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap">
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { background: #0f172a; color: #f1f5f9; font-family: 'Poppins', sans-serif; }
    .navbar { position: fixed; top: 0; left: 0; width: 100%; background: #101010; padding: 0 40px; z-index: 100; border-bottom: 2px solid #00bcd4; display: flex; align-items: center; justify-content: space-between; height: 70px; box-sizing: border-box; }
    .navbar-title { font-size: 1.8rem; font-weight: 700; color: #00bcd4; letter-spacing: 1.5px; }
    .navbar-actions { display: flex; align-items: center; gap: 16px; }
    .navbar-btn { background: #00bcd4; color: #fff; border: none; border-radius: 4px; padding: 8px 18px; font-size: 0.95rem; font-weight: 500; cursor: pointer; transition: background 0.2s; text-decoration: none; }
    .navbar-btn:hover { background: #0097a7; }
    .main-content { max-width: 1200px; margin: 100px auto 40px auto; padding: 0 20px; }
    .page-header { text-align: center; margin-bottom: 30px; }
    .page-header h1 { color: #00bcd4; font-size: 2.5rem; margin-bottom: 10px; }
    .bookings-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(350px, 1fr)); gap: 20px; }
    .booking-card { background: #1e293b; border-radius: 12px; padding: 20px; border: 1px solid #334155; }
    .booking-card h3 { color: #14b8a6; margin-bottom: 15px; }
    .booking-info { margin-bottom: 10px; }
    .booking-info strong { color: #94a3b8; }
    .booking-info span { color: #f1f5f9; }
    .ticket-id { background: #0f172a; padding: 8px; border-radius: 6px; font-family: monospace; font-size: 0.9rem; margin: 10px 0; }
    .seats-display { display: flex; flex-wrap: wrap; gap: 5px; margin: 10px 0; }
    .seat-badge { background: #14b8a6; color: #0f172a; padding: 4px 8px; border-radius: 4px; font-size: 0.8rem; font-weight: 600; }
    .status-badge { display: inline-block; padding: 4px 12px; border-radius: 20px; font-size: 0.8rem; font-weight: 600; }
    .status-confirmed { background: #22c55e; color: white; }
    .status-cancelled { background: #ef4444; color: white; }
    .no-bookings { text-align: center; padding: 40px; color: #94a3b8; }
    .no-bookings h3 { color: #14b8a6; margin-bottom: 10px; }
  </style>
</head>
<body>
  <div class="navbar">
    <div class="navbar-title">CINE-BOOK</div>
    <div class="navbar-actions">
      <span>Welcome, <%= user.email %></span>
      <a href="/movies" class="navbar-btn">Movies</a>
      <a href="/my-bookings" class="navbar-btn">My Bookings</a>
      <a href="/logout" class="navbar-btn">Logout</a>
    </div>
  </div>

  <div class="main-content">
    <div class="page-header">
      <h1>🎫 My Bookings</h1>
      <p>View your movie ticket bookings</p>
    </div>

    <% if (bookings && bookings.length > 0) { %>
      <div class="bookings-grid">
        <% bookings.forEach(function(booking) { %>
          <div class="booking-card">
            <h3><%= booking.movieId ? booking.movieId.title : 'Unknown Movie' %></h3>
            
            <div class="ticket-id">
              <strong>Ticket ID:</strong> <%= booking.ticketId %>
            </div>
            
            <div class="booking-info">
              <strong>Show Time:</strong> <span><%= booking.showTime %></span>
            </div>
            
            <div class="booking-info">
              <strong>Booking Date:</strong> <span><%= new Date(booking.bookingDate).toLocaleDateString() %></span>
            </div>
            
            <div class="booking-info">
              <strong>Seats:</strong>
              <div class="seats-display">
                <% booking.seats.forEach(function(seat) { %>
                  <span class="seat-badge"><%= seat %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="booking-info">
              <strong>Total Amount:</strong> <span>₹<%= booking.totalAmount %></span>
            </div>
            
            <div class="booking-info">
              <strong>Status:</strong> 
              <span class="status-badge status-<%= booking.status %>">
                <%= booking.status === 'confirmed' ? 'Confirmed' : 'Cancelled' %>
              </span>
            </div>
            
            <% if (booking.status === 'confirmed') { %>
              <div style="margin-top: 15px;">
                <button onclick="cancelBooking('<%= booking._id %>')" style="background: #ef4444; color: white; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer;">
                  Cancel Booking
                </button>
              </div>
            <% } %>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="no-bookings">
        <h3>No bookings found</h3>
        <p>You haven't made any bookings yet.</p>
        <a href="/movies" class="navbar-btn" style="margin-top: 15px; display: inline-block;">Browse Movies</a>
      </div>
    <% } %>
  </div>

  <script>
    async function cancelBooking(bookingId) {
      if (!confirm('Are you sure you want to cancel this booking?')) return;
      
      try {
        const response = await fetch(`/api/bookings/${bookingId}/cancel`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' }
        });
        
        if (response.ok) {
          alert('Booking cancelled successfully!');
          location.reload();
        } else {
          alert('Failed to cancel booking');
        }
      } catch (error) {
        console.error('Error cancelling booking:', error);
        alert('Error cancelling booking');
      }
    }
  </script>
</body>
</html>

