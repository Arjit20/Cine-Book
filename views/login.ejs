<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - CINE-BOOK</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #0a0a0a;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        /* Animated Background */
        .background-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            overflow: hidden;
        }

        .animated-text {
            position: absolute;
            font-size: 160px;
            font-weight: 900;
            color: rgba(229, 9, 20, 0.08);
            white-space: nowrap;
            animation: scrollText 30s linear infinite;
            letter-spacing: 40px;
            text-transform: uppercase;
            pointer-events: none;
            font-family: 'Inter', sans-serif;
            text-shadow: 0 0 40px rgba(229, 9, 20, 0.15);
        }

        .text-line-1 {
            top: 5%;
            animation-delay: 0s;
        }

        .text-line-2 {
            top: 25%;
            animation-delay: -8s;
        }

        .text-line-3 {
            top: 45%;
            animation-delay: -16s;
        }

        .text-line-4 {
            top: 65%;
            animation-delay: -12s;
        }

        .text-line-5 {
            top: 85%;
            animation-delay: -4s;
        }

        @keyframes scrollText {
            from {
                transform: translateX(-100%);
            }
            to {
                transform: translateX(100vw);
            }
        }

        /* Gradient Orbs */
        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.2;
            animation: floatOrb 20s infinite ease-in-out;
        }

        .orb1 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, #e50914, #b20710);
            top: -200px;
            right: -200px;
            animation-delay: 0s;
        }

        .orb2 {
            width: 450px;
            height: 450px;
            background: radial-gradient(circle, #e50914, #8b0000);
            bottom: -200px;
            left: -200px;
            animation-delay: -10s;
        }

        .orb3 {
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, #ff1744, #e50914);
            top: 50%;
            left: 50%;
            animation-delay: -5s;
        }

        @keyframes floatOrb {
            0%, 100% {
                transform: translate(0, 0) scale(1);
            }
            25% {
                transform: translate(50px, -50px) scale(1.1);
            }
            50% {
                transform: translate(-30px, 30px) scale(0.9);
            }
            75% {
                transform: translate(20px, 40px) scale(1.05);
            }
        }

        /* Container */
        .container {
            width: 100%;
            max-width: 460px;
            padding: 20px;
            z-index: 10;
            position: relative;
        }

        .login-card {
            background: rgba(10, 10, 10, 0.75);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(229, 9, 20, 0.15);
            border-radius: 24px;
            padding: 48px 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.8), 0 0 80px rgba(229, 9, 20, 0.1);
            animation: slideUp 0.6s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Logo */
        .logo-section {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 36px;
            animation: logoGlow 3s ease-in-out infinite;
        }

        @keyframes logoGlow {
            0%, 100% {
                filter: drop-shadow(0 0 10px rgba(229, 9, 20, 0.3));
            }
            50% {
                filter: drop-shadow(0 0 20px rgba(229, 9, 20, 0.6));
            }
        }

        .logo-icon {
            width: 52px;
            height: 52px;
            background: linear-gradient(135deg, #e50914 0%, #b20710 100%);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 14px;
            font-size: 26px;
            font-weight: 900;
            color: white;
            box-shadow: 0 10px 30px rgba(229, 9, 20, 0.5);
            letter-spacing: -1px;
        }

        .logo-text {
            font-size: 32px;
            font-weight: 900;
            background: linear-gradient(135deg, #fff 0%, #e50914 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -1px;
        }

        /* Header */
        h1 {
            font-size: 32px;
            color: #fff;
            margin-bottom: 8px;
            text-align: center;
            font-weight: 700;
            letter-spacing: -0.5px;
        }

        .subtitle {
            text-align: center;
            color: rgba(255, 255, 255, 0.5);
            font-size: 15px;
            margin-bottom: 32px;
            font-weight: 400;
        }

        /* Messages */
        .error-message {
            color: #ff4444;
            margin-bottom: 24px;
            text-align: center;
            padding: 12px 16px;
            background: rgba(255, 68, 68, 0.1);
            border: 1px solid rgba(255, 68, 68, 0.2);
            border-radius: 10px;
            font-size: 14px;
            font-weight: 500;
        }

        .success-message {
            color: #22c55e;
            margin-bottom: 24px;
            text-align: center;
            padding: 12px 16px;
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.2);
            border-radius: 10px;
            font-size: 14px;
            font-weight: 600;
        }

        /* Form */
        .form-group {
            margin-bottom: 24px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 13px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        input {
            width: 100%;
            padding: 14px 16px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            color: #fff;
            font-size: 15px;
            transition: all 0.3s ease;
            outline: none;
            font-family: 'Inter', sans-serif;
        }

        input::placeholder {
            color: rgba(255, 255, 255, 0.3);
        }

        input:focus {
            background: rgba(255, 255, 255, 0.08);
            border-color: #e50914;
            box-shadow: 0 0 0 4px rgba(229, 9, 20, 0.2);
        }

        /* Password Toggle */
        .password-toggle {
            position: relative;
        }

        .password-toggle input {
            padding-right: 50px;
        }

        .toggle-btn {
            position: absolute;
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: rgba(255, 255, 255, 0.4);
            cursor: pointer;
            font-size: 20px;
            transition: color 0.2s;
            padding: 0;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .toggle-btn:hover {
            color: rgba(255, 255, 255, 0.7);
        }

        /* Remember & Forgot */
        .remember-forgot {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 28px;
            font-size: 13px;
        }

        .remember-forgot label {
            display: flex;
            align-items: center;
            margin-bottom: 0;
            cursor: pointer;
            text-transform: none;
            color: rgba(255, 255, 255, 0.6);
            font-weight: 500;
        }

        input[type="checkbox"] {
            width: 18px;
            height: 18px;
            margin-right: 8px;
            cursor: pointer;
            accent-color: #e50914;
        }

        /* Role Selection */
        .role-selection {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-bottom: 12px;
        }

        .role-option {
            display: flex;
            align-items: center;
            padding: 16px;
            background: rgba(255, 255, 255, 0.05);
            border: 2px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
        }

        .role-option:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .role-option input[type="radio"] {
            margin: 0;
            margin-right: 12px;
            cursor: pointer;
            width: 18px;
            height: 18px;
            accent-color: #e50914;
        }

        .role-option input[type="radio"]:checked + .role-label {
            color: #e50914;
            font-weight: 700;
        }

        .role-label {
            display: flex;
            flex-direction: column;
            font-size: 14px;
            font-weight: 600;
            color: rgba(255, 255, 255, 0.7);
            transition: color 0.2s;
        }

        .role-desc {
            font-size: 11px;
            color: rgba(255, 255, 255, 0.4);
            margin-top: 4px;
            font-weight: 400;
        }

        .role-option input[type="radio"]:checked ~ .role-label .role-desc {
            color: rgba(255, 255, 255, 0.6);
        }

        .forgot-link {
            color: rgba(255, 255, 255, 0.6);
            text-decoration: none;
            transition: color 0.2s;
            font-weight: 500;
        }

        .forgot-link:hover {
            color: #e50914;
        }

        /* Button */
        .btn-login {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #e50914 0%, #b20710 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 15px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
            font-family: 'Inter', sans-serif;
        }

        .btn-login::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-login:hover::before {
            left: 100%;
        }

        .btn-login:hover {
            box-shadow: 0 12px 40px rgba(229, 9, 20, 0.6);
            transform: translateY(-2px);
        }

        .btn-login:active {
            transform: translateY(1px);
        }

        /* Loading */
        .loading {
            text-align: center;
            color: white;
            margin-top: 20px;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 3px solid rgba(255, 255, 255, 0.1);
            border-top-color: #e50914;
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
            margin: 0 auto 12px;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* Sign Up Link */
        .signup-link {
            text-align: center;
            margin-top: 28px;
            color: rgba(255, 255, 255, 0.5);
            font-size: 14px;
            font-weight: 400;
        }

        .signup-link a {
            color: #e50914;
            text-decoration: none;
            font-weight: 700;
            transition: color 0.2s;
        }

        .signup-link a:hover {
            color: #f40612;
            text-decoration: underline;
        }

        /* Back to Home */
        .back-home {
            position: absolute;
            top: 32px;
            left: 32px;
            z-index: 100;
        }

        .back-home a {
            display: flex;
            align-items: center;
            gap: 8px;
            color: rgba(255, 255, 255, 0.6);
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s;
            padding: 10px 18px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            backdrop-filter: blur(10px);
        }

        .back-home a:hover {
            color: #fff;
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(229, 9, 20, 0.5);
        }

        /* Responsive */
        @media (max-width: 480px) {
            .login-card {
                padding: 36px 28px;
            }

            h1 {
                font-size: 28px;
            }

            .logo-icon {
                width: 42px;
                height: 42px;
                font-size: 20px;
            }

            .logo-text {
                font-size: 24px;
            }

            .animated-text {
                font-size: 100px;
                letter-spacing: 20px;
            }

            .back-home {
                top: 20px;
                left: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="background-container">
        <div class="orb orb1"></div>
        <div class="orb orb2"></div>
        <div class="orb orb3"></div>
        <div class="animated-text text-line-1">CINE-BOOK</div>
        <div class="animated-text text-line-2">CINE-BOOK</div>
        <div class="animated-text text-line-3">CINE-BOOK</div>
        <div class="animated-text text-line-4">CINE-BOOK</div>
        <div class="animated-text text-line-5">CINE-BOOK</div>
    </div>

    <div class="back-home">
        <a href="/">
            <span>←</span>
            <span>Back to Home</span>
        </a>
    </div>

    <div class="container">
        <div class="login-card">
            <div class="logo-section">
                <div class="logo-icon">CB</div>
                <div class="logo-text">CINE-BOOK</div>
            </div>

            <h1>Welcome Back</h1>
            <p class="subtitle">Sign in to book your favorite movies</p>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>
            
            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message" id="serverSuccess">
                    <%= success %>
                </div>
            <% } %>

            <div class="success-message" id="successMsg" style="display:none;">
                ✓ Login successful! Redirecting...
            </div>

            <form id="loginForm" method="POST" action="/auth/login?redirect=<%= encodeURIComponent(redirect || '/movies') %>">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email"
                        placeholder="your@email.com" 
                        required
                    >
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <div class="password-toggle">
                        <input 
                            type="password" 
                            id="password" 
                            name="password"
                            placeholder="Enter your password" 
                            required
                        >
                        <button type="button" class="toggle-btn" id="togglePassword">
                            ⊙
                        </button>
                    </div>
                </div>

                <!-- Role Selection for Login -->
                <div class="form-group">
                    <label>Login as:</label>
                    <div class="role-selection">
                        <label class="role-option">
                            <input type="radio" name="role" value="user" checked>
                            <span class="role-label">👤 User</span>
                            <span class="role-desc">Browse & book</span>
                        </label>
                        <label class="role-option">
                            <input type="radio" name="role" value="admin">
                            <span class="role-label">🔐 Admin</span>
                            <span class="role-desc">Manage system</span>
                        </label>
                    </div>
                </div>

                <div class="remember-forgot">
                    <label>
                        <input type="checkbox" name="remember">
                        Remember me
                    </label>
                    <a href="#" class="forgot-link">Forgot Password?</a>
                </div>

                <button type="submit" class="btn-login" id="loginBtn">
                    Sign In
                </button>

                <div class="loading" id="loading" style="display:none;">
                    <div class="spinner"></div>
                    <p>Signing in...</p>
                </div>
            </form>

            <div class="signup-link">
                Don't have an account? <a href="/auth/register">Create Account</a>
            </div>
        </div>
    </div>

    <script>
        // Password toggle functionality
        const togglePassword = document.getElementById('togglePassword');
        const passwordInput = document.getElementById('password');

        togglePassword.addEventListener('click', function() {
            const type = passwordInput.type === 'password' ? 'text' : 'password';
            passwordInput.type = type;
            this.textContent = type === 'password' ? '⊙' : '⊗';
        });

        // Form submission handler
        const loginForm = document.getElementById('loginForm');
        const loginBtn = document.getElementById('loginBtn');
        const loading = document.getElementById('loading');

        loginForm.addEventListener('submit', function() {
            loginBtn.style.display = 'none';
            loading.style.display = 'block';
        });

        // Input focus effects
        const inputs = document.querySelectorAll('input[type="email"], input[type="password"]');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transition = 'transform 0.3s ease';
                this.parentElement.style.transform = 'scale(1.01)';
            });

            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'scale(1)';
            });
        });
    </script>
</body>
</html>